---
source_handle: connect-npm-current
fetched: 2026-07-07
source_url: https://registry.npmjs.org/@connectrpc/connect/latest
provenance: source-direct
---

# Attestation: npm registry — @connectrpc/connect latest

## Summary

The npm registry latest endpoint for `@connectrpc/connect` reports version 2.1.2. The package describes itself as "Type-safe APIs with Protobuf and TypeScript", is Apache-2.0 licensed, and uses the GitHub repository `connectrpc/connect-es` under `packages/connect`. It declares peer dependency `@bufbuild/protobuf` version `^2.7.0`.

## Key passages

1. The JSON object reports `"name": "@connectrpc/connect"` and `"version": "2.1.2"`.

2. The `description` field says: "Type-safe APIs with Protobuf and TypeScript."

3. The `repository` field points to `git+https://github.com/connectrpc/connect-es.git` with directory `packages/connect`.

4. The `peerDependencies` field lists `"@bufbuild/protobuf": "^2.7.0"`.

5. The package `exports` include protocol-specific subpaths: `./protocol`, `./protocol-connect`, `./protocol-grpc`, and `./protocol-grpc-web`.
