---
source_handle: openapi-generator-typescript
fetched: 2026-06-28
source_url: https://raw.githubusercontent.com/OpenAPITools/openapi-generator/master/docs/generators/typescript.md
provenance: source-direct
substrate_confidence: source-direct
---

# Attestation: OpenAPI Generator TypeScript generator docs

## Summary

OpenAPI Generator documents a `typescript` generator whose metadata identifies it as a Fetch API TypeScript client generator. The metadata table marks the generator as experimental and the help text says it generates a TypeScript client library marked beta.

## Key passages

1. From the metadata table:

> generator name | typescript | pass this to the generate command after -g

2. From the metadata table:

> generator stability | EXPERIMENTAL

3. From the metadata table:

> generator type | CLIENT

4. From the metadata table:

> helpTxt | Generates a TypeScript client library using Fetch API (beta).
