---
source_handle: openapi-generator-rust
fetched: 2026-06-28
source_url: https://raw.githubusercontent.com/OpenAPITools/openapi-generator/master/docs/generators/rust.md
provenance: source-direct
substrate_confidence: source-direct
---

# Attestation: OpenAPI Generator Rust generator docs

## Summary

OpenAPI Generator documents a `rust` generator whose metadata identifies it as a Rust client generator. The metadata table marks generator stability as stable while its help text says it generates a Rust client library marked beta.

## Key passages

1. From the metadata table:

> generator name | rust | pass this to the generate command after -g

2. From the metadata table:

> generator stability | STABLE

3. From the metadata table:

> generator type | CLIENT

4. From the metadata table:

> helpTxt | Generates a Rust client library (beta).
