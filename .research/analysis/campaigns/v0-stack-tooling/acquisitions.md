---
provenance: agent-synthesis
updated: 2026-07-07
campaign: v0-stack-tooling
---

# Acquisition candidates — v0-stack-tooling

Consolidated from the four specialist returns. No blocking gaps surfaced. Three enriching candidates, each source-bound (pointing at a fetched source that names them). Promotion into the `.work/` `research-acquisition-queue` is operator-confirmed at the handoff gate (not auto-fired).

## Enriching

### connect-node × tonic interop spike / conformance record
- **source:** `connect-node-client-transports` / `tonic-docs-current` (fetched this engagement)
- **class:** `ingestible` (operator-generated spike) / `portal` (if a public conformance record exists)
- **web-availability:** unknown — generic Connect/tonic docs establish protocol compatibility but not this exact library pair under Patchbay service shapes; no fetched source records the pair's interop under TLS/streaming/error-metadata
- **completes:** operational confidence for `@connectrpc/connect-node` `createGrpcTransport()` against tonic-generated Rust services — metadata, TLS, streaming, and error-detail interop under Patchbay service shapes. Resolves the one residual operational gap the `internal-seam-connect` facet flagged.

### Codex app-server generated JSON Schema / TypeScript
- **source:** `codex-appserver-current` (fetched this engagement; the specialist attested the README documents version-matched generated schemas)
- **class:** `primary-doc` (generated schema artifact)
- **web-availability:** not a static web page — generated by the exact Codex CLI binary version Patchbay evaluates
- **completes:** exact app-server method/field/state schema beyond the README prose attested here. Deepens the reference-control-planes facet if Codex's control-plane shape becomes an adapter-design reference.

### OpenCode `/doc` OpenAPI 3.1 document
- **source:** `opencode-server-docs-current` (fetched this engagement; the specialist attested the server publishes OpenAPI 3.1 at `/doc`)
- **class:** `primary-doc` (generated server contract)
- **web-availability:** available from a running pinned OpenCode server at `/doc`, not a stable single public URL
- **completes:** exact request/response/event surface for adapter design. Deepens the reference-control-planes facet if OpenCode's server shape becomes an adapter-design reference.

## Blocking

None. No load-bearing source was inaccessible in a way that gated a specialist's claims. The two `unreachable-source` lint flags (`cursor-cloud-agents-current`, `gnu-screen-session-persistence`) are attested-but-not-currently-refetchable, not acquisition-gated gaps — the attestations stand as record-of-fetch; see `parent.md` "Verification notes".
