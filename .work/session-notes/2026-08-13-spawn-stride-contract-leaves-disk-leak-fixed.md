# Session note — 2026-08-13 (spawn-stride contract leaves 4/6 done; Leaf 6 implemented pending integration fix; disk leak rooted out)

A durable handoff note for the next session. Read this, then `.work/next-run-prompt.md`, before continuing.

## What happened this session

Autopilot resumed the **spawn stride** (scope: `research-handoff-spawn` 16 children + `research-handoff-pi-adapter-capability` 6 children, top-level orchestration per AGENTS.md — no delegated autopilot; all workers/reviewers `openai-codex/gpt-5.6-sol` explicit, thorough review weight, deep lane on `[verification]` children). **4 of the 6 spawn-side contract leaves went done** — each via implement → deep review → fix → convergence, with every review finding mutation-tested. **Leaf 6 is implemented but needs an integration fix pass** (6 BLOCKERs, findings durably recorded). Two environment failures were root-caused and fixed, and the Pi/durability scope blocker was resolved by the operator (option A).

### Contract leaves done (commits in order)

- **`logical-target-identity-contract`** ✅ `7e95273` → fix `62ed1cb` → done `c41a392`. Review (NEEDS-FIX, 3 MATERIAL): design field name `external_runtime` vs `external_runtime_ref`; checkpoint-recovery ownership unproven e2e (mutating materialization→`Vec::new()` left all tests green — classic vacuous property); `CurrentAlreadyAssigned` slot guard not mutation-tested. All fixed; mutants now fail named tests.
- **`continuation-payload-authority-contract`** ✅ `46f896c` → fix `a184e44` → done `dba571e`. Review (**BLOCKER** + 2 MATERIAL): the live submit path accepted a spawn-Grant-only continuation and durably persisted it — BLOCKER 1's exact escalation, executable. Fixed by the same **guard-until-downstream-ready** pattern used later: the submit path rejects continuation intents (`unsupported_command`) before grant/append until `fleet-spawn-target-resolution` wires compound selection; fresh spawn stays enabled. Raw-wire scanner now fails closed on unknown fields (was `_ => {}`); the acceptance↔authority import cycle broken via neutral `core/src/contract_validation.rs`.
- **`claim-registry-contract`** ✅ `7d7c9cc` → fix `6324a57` → done `03c778b`. Review (**BLOCKER** + MATERIAL): evidence validation was *nominal* — an empty sibling `Observation` was accepted as all three no-effect proofs, as prior-N liveness, and as promotion evidence (any old event could release/poison/promote). Fixed: strict validation (exact stored-event bytes, discriminator, claim correlation, `accepted_lsn <= evidence_lsn`, adapter binding) and **evidence-dependent transitions guarded** until Leaf 5/6 register typed envelopes; checkpoint disposition-manufacturing closed (raw decode private; recovery rebuilds from the durable log).
- **`crash-external-effect-evidence-contract`** ✅ `86d1c9f` → fix `bc7cc9d` → done `9d3f7f3`. Review (**2 BLOCKERs** + 4 surviving mutants): (1) empty `CorePreDeliveryTerminalProof` let `execution_outcome_unknown` — the canonical *ambiguous* outcome — release a claim; (2) an obsolete no-effect proof cleared a later-poisoned/delivered claim. Fixed: core proof requires an exact durable terminal `CommandTransition`; poisoned releases require post-poison proof + reject later contradictory delivery/effect evidence (watermark); 4 mutants killed (missing-evidence-id-as-silence, source-adapter↔claim-adapter binding, fresh-claim-prior-phase, exact-prior-N identity). Evidence gate is now airtight.

### Leaf 6 — implemented, at `review` with 6 BLOCKERs (do NOT re-implement; integrate)

`runtime-evidence-promotion-contract` — commit `eee06b2` at `stage: review`. The worker built the promotion/quarantine/ClaimedSuccessor contracts, a **genuinely sound one-transaction atomic SQLite promotion append** (`append_spawn_promotion_audited` — reviewer confirmed no two-append fallback; the design's riskiest assumption is satisfied), and focused tests. **But it is not wired into the production paths.** The deep review (xhigh) found 6 BLOCKERs: (1) `ClaimedSuccessor` never validates the current durable attachment and has **no production call site** (managed reports still go through ordinary `SessionRegistered`/`SessionGenerationBumped`); (2) the **forbidden raw stale-Observation replay shape still exists** (BLOCKER 5 not yet resolved — a regression test even asserts the legacy shape); (3) quarantine admits malformed/untyped payloads (`[0xff]` committed via generic audited append); (4) promotion can mint descendant authority for the **wrong subject** (authority laundering — not bound to the exact spawner); (5) generic storage routes bypass the dedicated promotion append (`SpawnPromotionCommitted::default()` committed via `append_audited`) and the test fixture promotion isn't authority-replayable; (6) the ordered authority→session→claim→command fold has **no replay call site** (server aggregate has no `SpawnClaimRegistry`). **Full findings + fix scope: `.work/active/reviews/leaf6-runtime-evidence-promotion-review-2026-08-13.md` — read it first when resuming.** The fix is integration + boundary-exclusivity + semantic-validity, not redesign.

### Environment failures rooted out (not papered over)

1. **ENOSPC root-cause + fix** (`f35eda9`). The recurring 60G+ balloon was **not** `/tmp` (848M) — root FS 99% full from `target/` 61G: 43G stable `debug` build cache + **18G / ~153K hidden `.tmp*-wal`/`.tmp*-shm` SQLite sidecars** in `target/test-tmp`. Root cause: `RusqliteStorage::open_in_memory()` used a bare `tempfile::NamedTempFile`, which tracks only the main DB file — SQLite WAL mode creates `-wal`/`-shm` siblings it never cleans. The test-tempfile fix (`[env] TMPDIR` + `#[ctor]`) had only **relocated** the leak out of `/tmp`. Fixed via a dedicated `TempDir` (drop removes DB + sidecars); full-workspace residue 18G→22M. Also: clearing must be `rm -rf target/test-tmp` (whole dir — the leaked files are hidden dotfiles, a `/*` glob misses them), and never post-build without touching a source first (`build.rs` creates the dir).
2. **CI red→green.** The continuation leaf's proto regen broke the pi-adapter e2e "generation bump" test (`expected generation 3, got 1` — misleading; the real cause was the e2e helper sending legacy text for its intentionally-invalid spawn, now hitting `validation_failed` before the phase under test). Bisected to `46f896c`; fixed (`0c8fb06`) by emitting a valid generated `SpawnRequest` so the test reaches its intended `target_not_found` path. **Lesson encoded in the resume prompt: workers/reviewers must run the FULL TS suite (operator-domain, pi-adapter), not just cargo + contracts drift.**

### Scope decision

**Pi/durability resolved: operator chose (A)** — `capability-manifest-durability-and-reconciliation-depth` (drafting, `parent: null`) is now **in scope**. It's a hard `depends_on` for the Pi feature + 3 Pi children (`manifest-profile`, `resource-reload-rehydration`, `lifecycle-conformance`). Plan: run `feature-design` on it at the Pi manifest gate (it blocks no spawn-side work).

### Operating lessons (now encoded in `.work/next-run-prompt.md`)

- **Proto-gen serialization:** `npm run gen` regenerates ALL committed Rust+TS bindings — two concurrent proto-editing leaves corrupt each other's gen output (it killed the first Wave-2 attempt alongside ENOSPC). All proto/contract leaves serialize; non-proto operation leaves (Rust core/server) can parallelize (cargo's workspace lock is safe).
- **Deep-lane reviews earn their cost:** every leaf this session shipped with at least one real BLOCKER/MATERIAL the implementer's green tests missed — the recurring failure mode is *contracts implemented in isolation, not wired into production paths* (Leaf 6's 6 BLOCKERs are all that pattern). Keep mutation-testing every security invariant; green tests are not proof.
- **Disk hygiene:** clear `target/test-tmp` (whole dir) between waves; `target/debug` is a stable cache — keep it.

## State at handoff

- HEAD pushed, tree clean; **CI green through `9d3f7f3`** (rust ✓ contracts-and-conformance ✓ typescript-suites ✓). Leaf 6 commit `eee06b2` + checkpoint docs are pushed but its CI run is the one in flight/next.
- Disk 13G free (92%), leak bounded (test-tmp 344M residue after the heaviest leaf).
- Spawn children: 4 done, `runtime-evidence-promotion` at review (fix pending), `cursor-authoritative-replacement` at implementing (untouched), 10 operation leaves queued, then Pi.
- **Resume point: fix Leaf 6's 6 BLOCKERs** (integration) → review → done; then `cursor-authoritative-replacement-contract` (Leaf 4, TS, depends identity only); then operation waves; then Pi with `capability-manifest-durability` at the manifest gate. Wave plan + plumbing details in `.work/next-run-prompt.md`.
