# Session note — 2026-07-30

**Scope:** Patchbay × token-commune × Workbench strategic exploration → resource-plane foundation implementation. Driven interactively, then under agile-workflow autopilot (review weight: thorough; workers on openai-codex/gpt-5.6-sol).

## The strategic arc

Started from "preparing to drive Patchbay toward token-commune." Cloned `block/buzz` to assess the space. **Two corrections worth remembering** (I misread twice, user caught both):

1. I kept reducing Buzz to "GPU-mesh only." Buzz is model/agent-agnostic (agents connect to the relay with identity like humans); the GPU mesh is one resource option. The real overlap is the *coordination plane*, which Buzz ships and the patchbay/token-commune pitch named as unbuilt.
2. I framed Buzz Mesh as the resource plane to compare against token-commune. Buzz is provider-agnostic throughout; its mesh is one feature, not its identity.

These corrections reshaped the analysis: Buzz is a shipping coordination workspace, not just a compute pool, and it can absorb patchbay-shaped features more easily than vice versa.

## The combined-surface vision → dissolved

Pitched a "Patchbay × Workbench × token-commune" combined surface, then put it through **five adversarial review rounds** (GPT-5.6 Sol + Kimi K3, cross-model). The vision dissolved under its own honesty:

- **Round 1–2:** the "three-product family" framing didn't hold — the composition was always Patchbay's delivery contract viewed alongside other things. The delivery contract *is* Patchbay.
- **Round 3–4:** the "typed composition" mechanism didn't exist (`TypedCorrelation` is a closed oneof; no external-reference variant). "Verified landing" was false (`completed` = adapter-reported only). Dual-durability Option B (git owns work state) dissolved the payoff into co-location-with-logging.
- **Round 5:** the honest residue was "one unbuilt pane" — not a vision. Parked it.

**The determination that survived:** Patchbay is the durable hub; Workbench and token-commune are the first two *adapter instances*, not co-equal substrates. Co-ownership is a convenience (negotiation-latency removal), not a structural requirement. The seams are generic, so value survives independent ownership. This matches Patchbay's existing adapter-neutral identity and where sole-maintainer control sits.

## Decisions worth remembering

- **Patchbay-as-hub, not product family.** Drive Patchbay toward adapter seams; Workbench/token-commune plug in.
- **OKF v0.2 is the candidate format for the reserved third adapter kind** (a foreign git-backed knowledge bundle is neither session nor operational resource under SPEC's admission rule). Recorded in the resource-plane epic; the capability-manifest feature keeps the target-category registry extensible so it's an additive future promotion, not a rearchitecture.
- **Option B: git owns work state; Patchbay owns the delivery contract.** Patchbay does not mutate work ledgers; correlation is convention-based. Dual durability dissolved.
- **The combined-view hypothesis** (does a delivery-contract + ledger + fuel view beat the incumbent?) is a mockup-first experiment if pressure materializes — parked, not killed.

## Durable artifacts

- `docs/PITCH-combined-surface.md` — the dissolved vision, honest about its own gaps.
- `.work/backlog/` parked findings: `idea-combined-surface-vision`, `idea-third-adapter-kind-foreign-data-source`, `idea-public-client-api-vs-split-deployment`, `idea-correlation-grounding-validation-leak`, `idea-ledger-pane-falsifiable-experiment`. Three threaded into active epics (resource-plane, public-compatibility).
- `idea-combined-surface-demand-research` was **deleted** — unanswerable at n=1 (the builder is the only operator).

## Resource-plane arc — delivered

The `epic-agent-operations-resource-plane` foundation epic was decomposed (5 features) with a Phase 4.6 mockup exploration (Resources destination + session runtime-context strip + mobile; provider/model pickers noted as sibling session-runtime scope). Then driven under autopilot:

| Feature | State |
|---|---|
| `resource-identity` | ✅ done (4 stories, thorough-reviewed) |
| `resource-state` | ✅ done (5 stories, thorough-reviewed, 3-pass convergence) |
| `capability-manifest` | ✅ done (3 stories, thorough-reviewed) |
| `cockpit-composition` | 🔄 implementing (designed, 4 stories) |
| `conformance` | ⏳ drafting |

The entire Rust protocol/storage foundation is real, reviewed code. resource-state's thorough loop alone closed 5 soundness gaps (UNKNOWN freshness, authoritative-unknown rejection, redeclaration degradation, snapshot discriminator, post-commit token fencing) — the kind of edge-case work thorough review exists for.

**Cockpit-composition mockups** at `.mockups/screens/epic-agent-operations-resource-plane/` (Resources destination, interactable runtime-context strip, mobile pill-buttons + bottom sheet). HTTP-served on port 8765 during the session (may be down by next session).

## Handoff / resume

Remaining: `cockpit-composition` (implementing → implement + thorough review), `conformance` (drafting → design + implement + review), then epic-level aggregate review + Phase 8 completion review.

Resume as a fresh harness goal:

> Use agile-workflow autopilot to drain `epic-agent-operations-resource-plane` until all in-scope active items are done or blocked. Review weight: thorough. Worker capability: highest (openai-codex/gpt-5.6-sol).

`cockpit-composition` is the next ready item; the foundation it consumes is landed.

## What I'd do differently

- I misread Buzz's scope twice before the user corrected me — I should have verified "what does Buzz's resource plane actually span" against the repo before asserting "GPU-only." Grounding before framing.
- The combined-surface vision took five review rounds to dissolve; the "co-location vs composition" distinction could have been pressure-tested earlier with one concrete cross-substrate workflow rather than abstract claims.
- resource-state needed three convergence passes; the stale-state/unknown invariants were under-specified in the initial design. Foundation soundness invariants deserve more up-front precision in the design pass.
