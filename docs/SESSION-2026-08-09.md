# Session note — 2026-08-09

**Scope:** the v1 strategy + research arc that followed the token-commune-observer drain (covered in `SESSION-2026-08-08.md`). Started from "what's the board state," became a deliberate step-back on v1.0 scope, then a grounded research campaign, then scope consolidation + queue prep for a fresh autopilot session. Closed with a 24G `/tmp` cleanup.

## The v1 reframe

The session's most consequential move was reframing **v1.0 = "the operator's real daily workflow (herdr + outpost_pi) runs on Patchbay, and NKlisch can deploy it"** — sharper than SPEC's abstract "additional operators can deploy it." Two known technical operators on small Tailscale infra, not a mass market. That reframe drove every scoping decision.

The brainstorm resolved the big levers:
- **Continue building: validated** — not by the foundation docs (which were set aside for this) but by surveying outpost_pi's recent bug-swatting: it's independently rebuilding Patchbay's core (durable-transcript-ownership, canonical ordering, hot-reload lifecycle, mobile "dedicated-ops model," identity durability) in a substrate that fights it. The bugs cluster exactly in Patchbay's surfaces.
- **The adapter question** ("why not just spawn CLIs + inject skills, becoming a harness?") resolved: the adapter is *where* the harness-logic lives so the **core stays a control plane** (agent-neutral, multi-machine, authority-bearing). Dropping the adapter = becoming a Pi-harness with no moat. token-commune (a 2nd adapter shape) already cashed the abstraction in.
- **Spawn = must** (herdr replacement); **mesh / Elicitations = out of v1** (operator prefers conversational `instruct`); **mobile = responsive-web must, native could**; **formal coverage blocks v1**.
- herdr = detached spawn/restart host; outpost_pi = pi-extension + relay + mobile app. The migration = Patchbay replaces both (spawn + mobile cockpit).

## The research campaign (`v1-control-plane-and-spawn`)

Exploratory 5-scout wave (Buzz, herdr, Claude Code, Pi, undiscovered-patchbay-adjacent) → full-rigor `.research/` campaign (3 specialists: peer-protocol-deep-dive / spawn-lifecycle / pi-adapter-probe; lint → cross-synthesis → adversarial-read → evaluator → spot-check; 2-pass convergence on the synthesis).

**Verdicts (corpus-bounded, provisional pending CodeAgent Mobile's backend):**
- **The moat holds — narrowly, as a composition.** No fetched peer combines durable-acceptance-through-terminal + caller-stable idempotency + incarnation fencing + authoritative recovery + resource-scoped authority. Mission Control (the closest namesake) is durable task/run governance + approvals but lacks the durable-operation contract (its spawn route mints a fresh id per call; its PRD names idempotent commands incomplete). Peers close individual primitives; Patchbay must not present those as unique.
- **Spawn lifecycle direction set** (proposed, 3 open forks): stable logical-target id + monotonic runtime generation; restart-as-continuation = new generation + old tombstoned; stale-generation fenced. Core owns identity/authority/durable-lifecycle/generation-fencing; adapter owns how continuation is realized.
- **Project/cwd seam DECIDED: adapter-owned in v1** (`target_spec`, no core `Project` entity).
- **Pi adapter grounded**: `get_entries(since)` cursor recovery (scoped, unknown-cursor resync); `/reload` ≠ runtime-upgrade → restart is the code-upgrade boundary; restart-as-continuation (no restart RPC — adapter owns terminate/respawn/reconcile).
- ~25 reusable conformance vectors; MC architectural direction harvested; CodeAgent Mobile backend = blocking acquisition.

Campaign bundle: `.research/analysis/campaigns/v1-control-plane-and-spawn/` (parent.md + 3 specialists + verification-checklist + acquisitions). Research-handoff emitted 5 work items (2 active, 3 backlog) + seeded the `research-acquisition-queue`.

## v1 scope consolidation + queue prep

Recorded the **must / could / post-v1 (no "should")** + definition-of-done in `epic-public-product-contract` (the v1 epic); parented spawn + Pi-adapter-capability under it. Must = spawn + Pi-adapter-capability + token-commune-control-attention + self-hosting + public-compatibility + publication-governance + executable-release-assurance + adapter-portability-proof + mobile-responsive cockpit.

Then scoped the accumulated backlog (40 items) → promoted 10 clusters (authority/sessions/resource/storage/verification/cockpit-ux features + test-tempfile/fleet-target/conformance/mc-harvest stories) + the `outpost-pi-pitfall-harvest` `[research]` item into active, clearing 28 backlog files. Left 12 (parked vision ideas + the acquisition queue).

## Disk cleanup

`/tmp` had accumulated **385K leaked test/render-temp entries = 23.3G** (the `test-tempfile-hygiene` backlog issue, far beyond its original 201K note). Cleaned stale non-system entries → disk **96%→78%** (6.9G→31G free); `/tmp` 25G→18M. The `test-tempfile-hygiene` item is demonstrably load-bearing for ops.

## Decisions worth remembering

- **v1 = workflow-migration + NKlisch**, not abstract "second operator." Every v1 scope call flows from this.
- **The moat is the composition**, not any primitive. Don't claim broad "durability"; claim the narrow five-axis composition. (The adversarial-review caught overclaim on exactly this — the precision discipline is load-bearing in research too, not just code.)
- **The adapter is where harness-logic lives so the core stays a control plane.** Becoming a harness = losing the moat.
- **Spawn = logical-target + generation + restart-as-continuation; Project seam = adapter-owned.** The generation fence is itself a moat component no peer has.
- **outpost_pi is accruing Patchbay-shaped complexity** — consolidate, don't duplicate. The `outpost-pi-pitfall-harvest` research item captures its lessons.
- **The `/tmp` test-temp leak is severe** (385K/23G) — `test-tempfile-hygiene` is real ops debt.

## Handoff / resume

The active queue is staged for a fresh autopilot session: **15 ready items** (incl. the `outpost-pi-pitfall-harvest` `[research]` engagement) + 5 blocked (the v1 product-shell features + Pi-adapter-capability).

Recommended fresh-session goal:

> Run the `outpost-pi-pitfall-harvest` `[research]` engagement first; thread its findings. Then use agile-workflow autopilot to drain the active queue (review weight: thorough; worker capability: highest). Spawn is the v1 critical path (unblocks Pi-adapter-capability → adapter-portability-proof).

Nothing is blocked on research. The 12 stay-set backlog items + the `research-acquisition-queue` are intentionally deferred. The v1 scope (must/could/post + definition-of-done) lives in `epic-public-product-contract`.
