{
  "vector_id": "retry-after-terminal-returns-existing",
  "property_id": "RetryAfterTerminalReturnsExisting",
  "promotion_status": "draft",
  "proto_fields_constrained": [
    "patchbay.Operation.command_id",
    "patchbay.Operation.idempotency_key",
    "patchbay.SubmissionResult.operation_state",
    "patchbay.SubmissionResult.command_id",
    "patchbay.SubmissionResult.deduplicated"
  ],
  "description": "Retrying a command with the same command id and idempotency key after terminalization returns the existing terminal record rather than creating a new candidate.",
  "input": {
    "existing_terminal_record": {
      "command_id": { "value": "cmd-terminal-001" },
      "idempotency_key": "idem-terminal-001",
      "operation_state": "OPERATION_STATE_COMPLETED",
      "terminal_lsn": { "value": 501 }
    },
    "retry_operation_type": "patchbay.Operation",
    "retry_operation": {
      "command_id": { "value": "cmd-terminal-001" },
      "authority_domain_id": { "value": "auth-main" },
      "kind": "OPERATION_KIND_INSTRUCT",
      "target_scope": { "kind": "TARGET_SCOPE_KIND_RUNTIME_SESSION", "runtime_session_id": { "value": "sess-004" }, "session_generation": { "value": 1 } },
      "idempotency_key": "idem-terminal-001",
      "submitted_at": "2026-07-06T00:10:00Z"
    }
  },
  "expected_outcome": {
    "result_type": "patchbay.SubmissionResult",
    "submission_result": {
      "outcome": "SUBMISSION_OUTCOME_ACCEPTED",
      "command_id": { "value": "cmd-terminal-001" },
      "operation_state": "OPERATION_STATE_COMPLETED",
      "accepted_lsn": { "value": 501 },
      "deduplicated": true
    },
    "new_terminal_candidate_created": false,
    "idempotency_key_reused": "idem-terminal-001"
  },
  "invariant_check": "RetryAfterTerminalReturnsExisting: terminal retry returns the existing terminal record; RetryReusesIdAndKey is exercised by the stable command-id/idempotency-key binding."
}
